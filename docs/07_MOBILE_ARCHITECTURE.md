# Mobile Architecture

## Stack
- Expo
- React Native
- TypeScript
- Expo Router
- Zustand
- TanStack Query
- Supabase JS

## Structure
- `app` for routes
- `components` for reusable UI
- `theme` for tokens
- `lib` for external clients
- `store` for local app state
