# 4UFitness Exact Vector Asset Set

This folder is reserved for editable SVG master assets.
The mobile app currently consumes the PNG exports in `assets/exact/icons` for maximum Expo compatibility.
Future pass can replace the PNG imports with SVG components if `react-native-svg` is added to the app.
